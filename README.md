# GK-Lemon-D

Lenor

## 简述

基于其他GKD订阅规则版本维护的英文版订阅规则。部分规则对文本进行匹配，使用英文语音时，将导致无法匹配原本的中文匹配规则。

本规则优先支持系统应用、应用默认跟随系统语言且无法修改的情况。其次支持应用存在中文版本，但主要语言版本为英文的情况。原则上不支持主要语言为中文，且可切换的情况。

本规则是其他规则的寄生虫，只在其他规则的基础上修改。不单独处理快照。
